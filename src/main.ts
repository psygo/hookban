import {
  HookbanBackground,
  HookbanContainer,
} from "./components/exports"

customElements.define(
  HookbanContainer.tag,
  HookbanContainer
)
customElements.define(
  HookbanBackground.tag,
  HookbanBackground
)
